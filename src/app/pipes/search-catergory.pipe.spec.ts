import { SearchCatergoryPipe } from './search-catergory.pipe';

describe('SearchCatergoryPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCatergoryPipe();
    expect(pipe).toBeTruthy();
  });
});
